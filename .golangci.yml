linters-settings:
  custom:
    loglinter:
      path: plugin/loglinter.so
      description: "Custom linter for enforcing log message rules"
      original-url: "github.com/RomanGolovinn/loglinter"

linters:
  disable-all: true
  enable:
    - loglinter